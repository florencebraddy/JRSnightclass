

function buttonPressed(value){
        document.getElementById("input-display").innerHTML = value
    }
    
/*state = {
  firstVal: "",
  secondVal: "",
  operation: underfined
}

function buttonPressed(newVal){

if newVal is the equal sympol call onEqualspressed ()

else if it is a number or it is a decimal, update state to firstval or secondval

state.firstVal = state.firstVal + (String(newVal))
document.getElementById("display-input").innerHTML = state.firstVal

}

if it is an operator, we want to operate state.operation 

else if 



function clear(){

state.firstVal =""
state.secondVal =""
operations = undefined

document.getElementById("input-display").innerHTML = state.firstVal
}

function equals-button (){

  if operation is '+'
  display state.firstVal + state.secondVal  

  else if operation is '*'
  display state.firstVal * state.secondVal  

  else if operation is '/'
  display state.firstVal / state.secondVal  

  else if operation is '-'
  display state.firstVal - state.secondVal  

  else ifoperation is undefined
my interpretation - what do i want this to say
}

*/